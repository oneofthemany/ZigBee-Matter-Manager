[package]
name = "zmm_telemetry"
version = "0.1.0"
edition = "2021"

[lib]
name = "zmm_telemetry"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.23", features = ["extension-module"] }
duckdb = { version = "1.1", features = ["bundled", "chrono"] }
parking_lot = "0.12"
chrono = "0.4"