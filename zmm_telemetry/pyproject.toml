[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "zmm_telemetry"
version = "0.1.0"
description = "Fast DuckDB appender for ZMM telemetry"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]