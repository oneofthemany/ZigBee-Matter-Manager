# Copy to keystore.properties (gitignored) and fill in with the values you
# chose when running keytool. See README.md for the command.
#
# storeFile is resolved relative to the android/ directory.

storeFile=zmm-release.jks
storePassword=CHANGE_ME
keyAlias=zmm
keyPassword=CHANGE_ME
