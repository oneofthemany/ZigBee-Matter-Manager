/build_root.sh
/frontend/therapy/node_modules/
/logs
/data/tts_models/
/data/tts_cache/
__pycache__/
*.pyc
.idea/

# Rust build artifacts (wheels are built in the container image)
zmm_telemetry/target/
zmm_eq/target/

# Databases & their sidecars — runtime/test data, never source (any location).
# Covers telemetry.duckdb, octopus.duckdb, test_*.duckdb, ad-hoc test DBs, plus
# the WAL/tmp sidecars and the healing-connect backups (*.wal.corrupt-<ts>).
*.duckdb
*.duckdb.wal
*.duckdb.tmp
*.wal
*.wal.corrupt-*
*.sqlite
*.sqlite3
*.db

# Secrets. config/config.yaml is tracked, so anything that must not reach the
# remote lives here instead (or in the environment). See config/secrets.yaml.example.
/config/secrets.yaml
